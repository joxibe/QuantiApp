import 'package:flutter/material.dart';

class AppColors {
  // Colores principales
  static const Color primary = Color(0xFF34D399); // Verde suave
  static const Color secondary = Color(0xFFF59E0B); // Amarillo cálido
  static const Color accent = Color(0xFF38BDF8); // Azul Turquesa
  
  // Colores de estado
  static const Color success = Color(0xFFA7F3D0); // Verde pastel
  static const Color warning = Color(0xFFFB923C); // Naranja brillante
  
  // Colores de fondo
  static const Color background = Color(0xFFF9FAFB); // Blanco roto / Gris claro
  static const Color cardBackground = Colors.white;
  
  // Colores de texto
  static const Color textPrimary = Color(0xFF1F2937); // Gris oscuro
  static const Color textSecondary = Color(0xFF6B7280); // Gris claro
  static const Color textLight = Colors.white;

  // Colores de transacciones
  static const Color income = Color(0xFF34D399); // Verde suave
  static const Color expense = Color(0xFFFB923C); // Naranja brillante

  // Colores de gradientes
  static const List<Color> primaryGradient = [
    Color(0xFF34D399),
    Color(0xFF38BDF8),
  ];

  static const List<Color> secondaryGradient = [
    Color(0xFFF59E0B),
    Color(0xFFFB923C),
  ];

  // Colores de sombras
  static const Color shadow = Color(0x1A000000);

  // Colores de bordes
  static const Color border = Color(0xFFE5E7EB);
} 